<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>

    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>

    <!-- Google Font (IBM Plex Sans) -->
    <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:wght@300;400;500;700&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'IBM Plex Sans', sans-serif;
        }
    </style>
</head>
<body class="bg-gray-50 text-gray-900">

    <!-- Page Container -->
    <div class="max-w-4xl mx-auto px-6 py-12">

        <!-- Header -->
        <header class="bg-white shadow-md rounded-lg p-4 mb-8">
            <div class="flex justify-between items-center">
                <h1 class="text-2xl font-semibold text-blue-600">Welcome, <%= user.email %></h1>
                <nav class="space-x-4">
                    <a href="/doctors" class="text-gray-700 hover:text-blue-500">Find a Doctor</a>
                    <a href="/bookings" class="text-gray-700 hover:text-blue-500">My Appointments</a>
                    <a href="/notifications" class="text-gray-700 hover:text-blue-500">Notifications</a>
                    <a href="/logout" class="bg-red-500 text-white px-4 py-2 rounded-lg hover:bg-red-600 transition">Logout</a>
                </nav>
            </div>
        </header>

        <!-- Main Content -->
        <main class="bg-white shadow-lg rounded-lg p-8">
            <h2 class="text-3xl font-semibold text-center mb-4">Your Dashboard</h2>
            <p class="text-gray-600 text-center mb-6">Manage your appointments and view notifications.</p>

            <!-- Upcoming Appointments -->
            <h3 class="text-2xl font-semibold mb-4">Upcoming Appointments</h3>
            <% if (bookings.length > 0) { %>
                <div class="grid gap-4">
                    <% bookings.forEach(booking => { %>
                        <div class="bg-gray-100 p-4 rounded-lg shadow-md">
                            <h4 class="text-xl font-medium">Dr. <%= booking.doctorName %></h4>
                            <p class="text-gray-600"><em><%= booking.doctorSpecialization %></em></p>
                            <p class="mt-2"><strong>Appointment Date:</strong> <%= booking.date %></p>
                            <p><strong>Status:</strong> <%= booking.status %></p>
                        </div>
                    <% }); %>
                </div>
            <% } else { %>
                <p class="text-gray-500 text-center">You don't have any upcoming appointments. Please 
                    <a href="/doctors" class="text-blue-500 hover:underline">book a doctor</a>.
                </p>
            <% } %>
        </main>

        <!-- Footer -->
        <footer class="text-center text-gray-500 text-sm mt-10">
            <p>&copy; 2025 Doctor Booking System</p>
        </footer>

    </div>

</body>
</html>
