<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Doctor Booking System</title>

    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>

    <!-- Google Font (IBM Plex Sans for a professional medical feel) -->
    <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:wght@300;400;500;700&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'IBM Plex Sans', sans-serif;
        }
    </style>
</head>
<body class="bg-gradient-to-b from-blue-50 to-white text-gray-900">

    <!-- Page Container -->
    <div class="max-w-5xl mx-auto px-6 py-12">

        <!-- Header -->
        <header class="bg-white shadow-md rounded-xl p-5 mb-10 flex justify-between items-center">
            <h1 class="text-3xl font-extrabold text-blue-600">Doctor Booking</h1>
            <nav class="space-x-4">
                <% if (typeof user !== 'undefined' && user) { %> 
                    <a href="/dashboard" class="text-gray-700 hover:text-blue-500 transition">Dashboard</a>
                    <a href="/logout" class="text-gray-700 hover:text-red-500 transition">Logout</a>
                <% } else { %> 
                    <a href="/login" class="text-gray-700 hover:text-blue-500 transition">Login</a>
                    <a href="/register" class="bg-blue-500 text-white px-5 py-2 rounded-lg hover:bg-blue-600 transition shadow-md">
                        Sign Up
                    </a>
                <% } %>
            </nav>
        </header>

        <!-- Hero Section -->
        <main class="bg-white shadow-xl rounded-2xl p-12 text-center">
            <h2 class="text-5xl font-extrabold text-blue-700 leading-tight">Find the Best Doctors for You</h2>
            <p class="text-gray-600 text-lg mt-4">Book appointments with top-rated doctors and receive the best medical care from the comfort of your home.</p>
            <a href="/doctors" class="mt-8 inline-block bg-blue-500 text-white text-lg font-semibold px-8 py-3 rounded-full shadow-lg hover:bg-blue-600 transition">
                Browse Doctors
            </a>
        </main>

        <!-- Features Section -->
        <section class="mt-16 grid grid-cols-1 md:grid-cols-3 gap-8 text-center">
            <div class="bg-white p-6 rounded-xl shadow-md hover:shadow-lg transition">
                <h3 class="text-2xl font-semibold text-blue-600">Qualified Doctors</h3>
                <p class="text-gray-600 mt-2">Consult with experienced and certified professionals.</p>
            </div>
            <div class="bg-white p-6 rounded-xl shadow-md hover:shadow-lg transition">
                <h3 class="text-2xl font-semibold text-blue-600">24/7 Support</h3>
                <p class="text-gray-600 mt-2">Book appointments and receive guidance anytime.</p>
            </div>
            <div class="bg-white p-6 rounded-xl shadow-md hover:shadow-lg transition">
                <h3 class="text-2xl font-semibold text-blue-600">Easy Booking</h3>
                <p class="text-gray-600 mt-2">Schedule appointments in just a few clicks.</p>
            </div>
        </section>

        <!-- Footer -->
        <footer class="text-center text-gray-500 text-sm mt-16">
            <p>&copy; 2025 Doctor Booking System. All rights reserved.</p>
        </footer>

    </div>

</body>
</html>
